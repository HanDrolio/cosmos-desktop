# COSM.OS Desktop v0.7.1 UI Refresh release trigger

This branch exists only to trigger the Windows packaging workflow. The workflow builds from the `ui-refresh` branch and publishes the `v0.7.1-ui-refresh` prerelease.
